{
  "name": "pong-game",
  "version": "1.0.0",
  "description": "A Pong game built with HTML, CSS, and TypeScript.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w & lite-server --baseDir=dist",
    "start": "lite-server --baseDir=dist",
    "go": "lite-server --baseDir=dist"
  },
  "dependencies": {},
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^4.5.2"
  }
}
